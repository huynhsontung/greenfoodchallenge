<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ecoone.mindfulmealplanner">

    <uses-permission android:name="android.permission.INTERNET">
    </uses-permission>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <service android:name="com.ecoone.mindfulmealplanner.Pledge.DailyPledgeService"/>
        <activity android:name=".InitialSetup.InitialSetupActivity">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
    </activity>
        <activity android:name=".MainActivity" />
        <activity
            android:name=".Setting.SettingsActivity"
            android:label="@string/title_activity_settings"
            android:parentActivityName=".MainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.ecoone.mindfulmealplanner.MainActivity" />
        </activity>
        <activity android:name=".Setting.AboutPageActivity">
            <intent-filter>
                <action android:name="openaboutpage" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity android:name=".DashBoard.Improve.ImproveActivity" />
        <activity
            android:name=".PlanList.PlanPagerActivity"
            android:theme="@style/AppTheme"></activity>
    </application>

</manifest>